# Whitelist: only Jupyter notebooks are tracked, everything else is ignored.
*
!*/
!*.ipynb

# Re-ignore known heavy/system directories even though !*/ allows traversal into them
.venv/
__pycache__/
.ipynb_checkpoints/
.DS_Store