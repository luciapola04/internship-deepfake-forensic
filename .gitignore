# Whitelist: only Jupyter notebooks and READMEs are tracked, everything else is ignored.
*
!*/
!*.ipynb
!README.md

# Re-ignore known heavy/system directories even though !*/ allows traversal into them
.venv/
__pycache__/
.ipynb_checkpoints/
.DS_Store